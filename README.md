# Slide Deck

Single-file cyberpunk presentation framework. Zero dependencies. Current example: Code & Bourbon developer meetup.

## Usage

Open `index.html` in a browser, or run `node server.js` for local features.

Arrow keys to navigate. `F` for fullscreen.

## License

MIT
